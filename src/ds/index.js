import { alpha as dsAlpha, color as dsColor } from './theme/color/utils'
import { depth as dsDepth, z as dsZ } from './theme/depth/utils'
import { space as dsSpace } from './theme/space/utils'
import { duration as dsDuration, easing as dsEasing } from './theme/motion/utils'
import {
  googleFontConfig as dsGoogleFontConfig,
  googleFontUrl as dsGoogleFontUrl,
  font as dsFont,
  fontWeight as dsWeight,
  fontSize as dsFontSize
} from './theme/typography/utils'

const dsFs = dsFontSize

export default {
  dsColor,
  dsAlpha,
  dsDepth,
  dsZ,
  dsSpace,
  dsGoogleFontConfig,
  dsGoogleFontUrl,
  dsFont,
  dsFontSize,
  dsFs,
  dsWeight,
  dsDuration,
  dsEasing
}
