import { config as color } from './color'
import { config as depth } from './depth'
import { config as space } from './space'
import { config as typography } from './typography'
import { config as motion } from './motion'

export default {
  color,
  typography,
  depth,
  space,
  motion,
  devMode: true,
  systemErrorColor: '#ff0000'
}
